#include "gameGolf.h" // Apenas inclua o header do seu jogo

int main(void)
{
    // A função gameGolf() agora gerencia todo o ciclo de vida do jogo.
    // Ela vai criar a janela, rodar o loop principal e liberar os recursos no final.
    gameGolf();

    // Nada mais é necessário aqui.
    return 0;
}